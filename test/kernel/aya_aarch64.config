# Minimal allnoconfig-based aarch64 kernel for Aya integration VMs.
#
# The pre-Bazel VM path uses Debian-built kernels and extracts their /boot
# configs. This file is intentionally not a Debian config; it is the explicit
# allowlist for the hermetic linux.bzl bootstrap kernel.

# Boot, initramfs, and the qemu serial console.
CONFIG_64BIT = y
CONFIG_ARM64 = y
CONFIG_BINFMT_ELF = y
CONFIG_BLK_DEV_INITRD = y
CONFIG_DEVTMPFS = y
CONFIG_DEVTMPFS_MOUNT = y
CONFIG_PRINTK = y
CONFIG_PROC_FS = y
CONFIG_PROC_SYSCTL = y
CONFIG_SYSFS = y
CONFIG_TMPFS = y
CONFIG_TTY = y
CONFIG_UNIX = y

CONFIG_SERIAL_AMBA_PL011 = y
CONFIG_SERIAL_AMBA_PL011_CONSOLE = y

# Kernel symbol/config visibility used by tracing, stack, BTF, and feature tests.
CONFIG_DEBUG_KERNEL = y
CONFIG_DEBUG_FS = y
CONFIG_DEBUG_INFO = y
CONFIG_DEBUG_INFO_BTF = y
CONFIG_DEBUG_INFO_DWARF5 = y
CONFIG_FHANDLE = y
CONFIG_KALLSYMS = y
CONFIG_KALLSYMS_ALL = y
CONFIG_STACKTRACE = y

# Namespaces and cgroup v2.
CONFIG_CGROUPS = y
CONFIG_CGROUP_BPF = y
CONFIG_CGROUP_DEVICE = y
CONFIG_CGROUP_NET_CLASSID = y
CONFIG_CGROUP_NET_PRIO = y
CONFIG_EVENTFD = y
CONFIG_NAMESPACES = y
CONFIG_NET_NS = y
CONFIG_USER_NS = y

# Networking, loopback, tc, XDP, LWT, sk_reuseport, and netfilter BPF coverage.
CONFIG_INET = y
CONFIG_IPV6 = y
CONFIG_LWTUNNEL = y
CONFIG_NET = y
CONFIG_NET_ACT_BPF = y
CONFIG_NET_CLS_ACT = y
CONFIG_NET_CLS_BPF = y
CONFIG_NET_CORE = y
CONFIG_NET_EGRESS = y
CONFIG_NET_INGRESS = y
CONFIG_NET_SCH_INGRESS = y
CONFIG_NET_SCHED = y
CONFIG_NETFILTER = y
CONFIG_NETDEVICES = y
CONFIG_PCI = y
CONFIG_XDP_SOCKETS = y

# BPF program/map coverage exercised by integration-test.
CONFIG_BPF = y
CONFIG_BPF_EVENTS = y
CONFIG_BPF_JIT = y
CONFIG_BPF_LSM = y
CONFIG_BPF_STREAM_PARSER = y
CONFIG_BPF_SYSCALL = y
CONFIG_BPF_UNPRIV_DEFAULT_OFF = n
CONFIG_KPROBES = y
CONFIG_KPROBE_EVENTS = y
CONFIG_PERF_EVENTS = y
CONFIG_FUNCTION_TRACER = y
CONFIG_SECURITY = y
CONFIG_SECURITY_NETWORK = y
CONFIG_SECURITYFS = y
CONFIG_SOCK_CGROUP_DATA = y
CONFIG_TRACING = y
CONFIG_FTRACE = y
CONFIG_FTRACE_SYSCALLS = y
CONFIG_UPROBES = y
CONFIG_UPROBE_EVENTS = y

# Module core support provides modprobe_path and /proc/sys/kernel/modprobe.
# The initrd still keeps modules empty unless a test provides them explicitly.
CONFIG_MODULES = y
CONFIG_MODPROBE_PATH = "/sbin/modprobe"
